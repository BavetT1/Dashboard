# Dashboard Мониторинга

Веб-интерфейс для централизованного сбора и отображения метрик по проектам клиентов.

## Возможности

- 📊 **Загрузка данных из Google Sheets** — автоматический парсинг финансовых метрик
- 📈 **Интерактивные графики** — прибыль, SMS, конверсии, заявки
- 🔄 **Автоматическое обновление** — данные кешируются на 24 часа
- 🌙 **Современный дизайн** — тёмная тема с glassmorphism эффектами
- 🔌 **Versioner интеграция** — отображение версий модулей (требует VPN)

## Клиенты

| Клиент | Статус |
|--------|--------|
| Т2 РФ | ✅ Активен |
| Билайн РФ | ✅ Активен |

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Google Credentials

Credentials уже настроены в `.env.local`. Убедитесь что сервис-аккаунт имеет доступ к таблицам:

```
monitor-bot@dashboard-monitor-485108.iam.gserviceaccount.com
```

### 3. Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница
│   ├── globals.css        # Глобальные стили
│   └── clients/[id]/      # Страница клиента
├── components/            # React компоненты
│   ├── ClientCard.tsx     # Карточка клиента
│   ├── MetricsChart.tsx   # Графики Chart.js
│   └── VersionsTable.tsx  # Таблица версий
└── lib/                   # Библиотеки
    ├── google-sheets.ts   # Google Sheets API
    ├── versioner.ts       # Versioner API
    ├── cache.ts           # Кеш-менеджер
    └── config.ts          # Конфигурация
```

## Добавление нового клиента

1. Откройте `src/lib/config.ts`
2. Добавьте нового клиента в массив `clients`:

```typescript
{
  id: 'new-client',
  name: 'Новый Клиент',
  spreadsheetId: 'GOOGLE_SHEET_ID',
  sheetName: 'Sheet1',
  versionerPath: '/new-client',
}
```

3. Предоставьте доступ сервис-аккаунту к Google таблице

## Технологии

- **Next.js 14** — React фреймворк
- **Chart.js** — библиотека графиков
- **googleapis** — Google API клиент
- **TypeScript** — типизация
