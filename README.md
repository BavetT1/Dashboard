# Dashboard Мониторинга

Веб-интерфейс для сбора и отображения метрик по SMS-рассылкам клиентов.

![Dashboard Screenshot](/Users/home/.gemini/antigravity/brain/cf561888-4e67-4632-b6ac-eb8aed2abbe2/beeline_rf_metrics_1769084440932.png)

## Возможности ver 1.1

- 📊 **Google Sheets интеграция** — автоматическая загрузка данных из таблиц
- 🔌 **Versioner интеграция** — отображение версий модулей из `versioner.cloud.c2m`
- 📅 **Фильтры по периодам** — месяц, неделя, всё время, произвольные даты
- 📉 **Интерактивные графики и таблицы** — с возможностью сворачивания секций
- 🔄 **Кеширование** — данные обновляются каждые 24 часа или принудительно

## Метрики

| Метрика | Источник | Описание |
|---------|----------|----------|
| **Всего SMS** | Столбец B | Количество доставленных SMS |
| **Всего заявок** | Столбец M | Итого заявок |
| **Прибыль веба** | Столбец S | Чистая прибыль |
| **PayPayout** | Столбец G | Сумма вознаграждения |
| **Стоимость за SMS** | G / B | Расчётная метрика (PayPayout / SMS) |

## Быстрый старт

### 1. Установка и запуск

```bash
cd Dashboard
npm install
npm run dev
```
Открыть http://localhost:3000

### 2. Обновление данных (Скрипт)

Данные кешируются на 24 часа. Чтобы **принудительно обновить** данные из Google Sheets и Versioner, выполните команду:

```bash
curl -X POST http://localhost:3000/api/refresh
```

Пример ответа:
```json
{
    "success": true,
    "message": "Данные обновлены",
    "clients": [
        {
            "id": "t2-rf",
            "name": "Т2 РФ",
            "recordsCount": 802,
            "lastDate": "2026-01-18"
        }
    ]
}
```

Можно добавить эту команду в CRON для регулярного обновления:
```bash
# Обновлять каждый час
0 * * * * curl -X POST http://localhost:3000/api/refresh
```

## Настройка

### Переменные окружения (.env.local)

```env
GOOGLE_CREDENTIALS_JSON='{...}' # JSON сервис-аккаунта Google
VERSIONER_URL=http://versioner.cloud.c2m
```

### Добавление клиента (src/lib/config.ts)

```typescript
export const clients: Client[] = [
    {
        id: 'new-client',          // URL slug (например /clients/new-client)
        name: 'Новый Клиент',      // Отображаемое имя
        spreadsheetId: '...',      // ID Google таблицы
        sheetName: 'Sheet1',       // (Опционально) Имя листа, иначе берется первый
        versionerPath: 't2ru',     // Проект в Versioner (t2ru, bee_ru, t2kz, bee_kz)
    }
];
```

## Структура проекта

```
src/
├── app/                      # Страницы и API
│   ├── api/refresh/          # Эндпоинт обновления данных
│   └── clients/[id]/         # Страница клиента
├── components/               # React компоненты
│   ├── ClientDashboard.tsx   # Основной компонент дашборда
│   ├── VersionsTable.tsx     # Таблица версий
│   ├── MetricsChart.tsx      # Графики
│   └── CollapsibleSection.tsx # Сворачиваемые блоки
└── lib/                      # Логика
    ├── google-sheets.ts      # Парсинг Google Sheets
    ├── versioner.ts          # API Versioner
    └── config.ts             # Конфигурация клиентов
```
